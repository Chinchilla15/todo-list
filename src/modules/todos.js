export default function ToDo(title, project, dueDate, priority, info){
    this.title = title;
    this.project = project;
    this.dueDate = dueDate;
    this.priority = priority;
    this.info = info;
};